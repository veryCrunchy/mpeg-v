# endpoints authorization; keep it the same across all apps
API_TOKEN=""

# Persist voice translation sessions so the bot can resume them after restarts.
# Use a path on persistent storage in production.
TRANSLATION_SESSIONS_FILE="/data/translation-sessions.json"
